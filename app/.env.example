# 0G Chain (testnet) — VERIFY current values in 0G docs
VITE_ZG_RPC=https://evmrpc-testnet.0g.ai
VITE_ZG_CHAIN_ID=16601

# 0G Storage indexer
VITE_INDEXER_RPC=https://indexer-storage-testnet-turbo.0g.ai

# Deployed AeonINFT address (from contracts deploy output)
VITE_AEON_CONTRACT=

# Pinned 0G Compute provider (run broker.inference.listService() once, pick a tested
# chatbot provider, paste its address here for demo reliability)
VITE_COMPUTE_PROVIDER=
VITE_DEFAULT_MODEL=glm-5
